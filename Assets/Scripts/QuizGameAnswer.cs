using UnityEngine;

public class QuizGameAnswer
{
    public string text;
    public Color color;

    public QuizGameAnswer(string text, Color color)
    {
        this.text = text;
        this.color = color;
    }
}